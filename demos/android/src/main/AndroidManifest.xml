<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.antgroup.antv.f2.samples"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ChartListActivity"
            android:label="@string/title_activity_chart_list"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".SampleActivity"
            android:label="@string/title_activity_sample"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".SampleActivityHorizontal"
            android:label="@string/title_activity_sample"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BridgeActivity"
            android:label="@string/title_activity_bridge"
            android:theme="@style/AppTheme" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>